services:
  postgres:
    image: postgres:16-alpine
    container_name: unitrip-database
    restart: unless-stopped

    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}

    ports:
      - "${DB_PORT}:${DB_PORT}"

    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./internal/infrastructure/database/schema.sql:/docker-entrypoint-initdb.d/schema.sql
      - ./internal/infrastructure/database/seed.sql:/docker-entrypoint-initdb.d/seed.sql

volumes:
  postgres_data:
